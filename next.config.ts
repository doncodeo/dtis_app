import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['placehold.co'], // Add 'placehold.co' to allowed image domains
  },
};

export default nextConfig;
